function [ P ] = Q2_2( T, pi0, Q , symb)
%T est le texte dont on doit calculer la vraisemblance
% symb est la liste des symboles
P = pi0*(T(1) == symb);
for i =2:length(T)
    P = P * (pi0*Q^(i-1))*(T(i) == symb);
end
end

